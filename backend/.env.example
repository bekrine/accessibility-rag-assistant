PORT=3000

DATABASE_URL=
# Set to "true" for most managed cloud Postgres providers (RDS, Supabase, etc.)
DATABASE_SSL=false

REDIS_HOST=127.0.0.1
REDIS_PORT=6379

RAG_SERVICE_URL=http://localhost:8000

HF_TOKEN=

# Shared secret with rag-service, protects internal/admin routes (issue PUT, queue introspection).
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
INTERNAL_API_KEY=

FRONTEND_ORIGIN=http://localhost:5173